# Installation

Install Node.js 20.19+ or 22.12+, then run `npm install`, `copy .env.example .env`, and `npm run dev`. The development server prints the local URL. Run `npm run build` before deployment and `npm run preview` to inspect the production build locally.

No backend is required to view the demo. Firebase is required for authentication and all form submissions.
