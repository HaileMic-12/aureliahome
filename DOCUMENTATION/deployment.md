# Deployment

Run `npm run build`; deploy `dist/` to Vercel, Netlify, Firebase Hosting, or any static host. Configure an SPA fallback so unknown paths serve `index.html`. Add all `VITE_FIREBASE_*` variables in the host settings, redeploy, and test a public form plus `/admin/login`. Deploy Firestore rules before enabling public traffic.
